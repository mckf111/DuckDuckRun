# DuckDuckRun 分阶段硬化计划

> **当前状态：第 1 阶段“事实基线与对抗式审查”已完成，结论为“保留并修复”。** 下一阶段不是扩功能，而是关闭发布可信度缺口。完整事实见 [`docs/review/first-principles-adversarial-audit.md`](docs/review/first-principles-adversarial-audit.md)。

| 阶段 | 目标 | 进入条件 | 退出条件 | 状态 |
|---|---|---|---|---|
| 1. 审查基线 | 实测玩法、设备模拟、质量门禁、线上制品和许可边界 | 已有可运行仓库 | 事实、评分、ADR、证据与设备矩阵入库 | **完成** |
| 2. 发布可信度硬化 | 修复浏览器回归、定义唯一制品源、补充质量命令和真机验收 | ADR-001 已接受 | `npm test` 全绿；线上 SHA 匹配；发布矩阵的 P0 单元格通过 | 待开始 |
| 3. 性能与可访问性收敛 | 用真机数据归因；降低高 DPR 风险；改善老少适配 | 阶段 2 发布链可追溯 | 目标设备 S1–S7 通过，性能/温度记录有结论 | 待开始 |
| 4. 内容与留存验证 | 以用户试玩与可回放数据验证重玩价值 | 基础体验可信可测 | 明确保留、迭代或削减的内容清单 | 待开始 |

## 阶段 2 的授权范围

阶段 2 可以对以下问题做最小必要修改：A-01 浏览器回归与预取契约，A-02 发布分支/制品漂移，A-03 可执行质量门禁，A-04 真机性能测量与必要裁剪，A-05 可访问性/老少适配，A-06 缓存原子性，A-07 随机局可重放，A-08 素材台账，以及 A-09 微信和真机验收。每个修改必须链接问题编号、复现命令、前后证据和回滚提交。

| 优先顺序 | 交付物 | 验收方式 |
|---:|---|---|
| 1 | A-01 的产品契约判定与最小修复 | `npm run test:browser`、`npm test`、CI 均绿；不以删除断言逃避问题 |
| 2 | 唯一制品源、线上版本标识与缓存策略 | 部署演练后线上提交 SHA/构建标识与批准提交匹配 |
| 3 | 无构建项目的明确质量定义 | 文档化的 `check` 命令可在冷环境复现；工具版本受控 |
| 4 | 设备矩阵 P0 样本 | 四类移动容器按 S1–S8 留痕，结果更新到 QA 文档 |
| 5 | 性能和可访问性最小修复 | 真机数据驱动；无障碍/字号/输入改动可回归 |
| 6 | 可复放随机局与素材 SBOM | 报告可含 seed/版本/输入摘要；第三方素材可追溯 |

## 明确禁止项

在 A-01/A-02 未关闭前，**禁止部署、禁止创建 PR、禁止向默认分支直接提交**。阶段 2 同时禁止新增关卡、道具、商业化、账户系统、后端、广告、PWA 离线系统、重型框架迁移、物理引擎替换和大规模美术重绘；禁止为了“让构建通过”而升级依赖。现有原生 Canvas/ESM 架构是默认保留对象，而不是待淘汰对象 [1]。

## 升级为重建评审的触发条件

只有当局部修复稳定超过约 18 人日（约等于重建下限的 60%），或真机/发布问题证明无法与当前状态机和静态部署边界隔离时，才提交新的重建 ADR。触发评审不等于批准重写；必须先比较重建后的功能迁移、资产许可重审、存档兼容和跨端回归成本 [1]。

## 参考资料

[1]: docs/architecture/ADR-001-target-architecture.md "保留并修复的架构决策、成本与回滚"
