# 换鸭、星级与图鉴照片优化

## 星级与换鸭（2026-09-07）

- 通关总星数来自各关历史最高成绩，跨难度不重复累加；金鸭仍为 15 星解锁，不扣星数。选关页将最高星级和累计印章分开，鸭铺解释星级与本地存档，结算页区分本次、最佳与总数。
- 原桌面基线能切换存档，但整页重绘把焦点移到标题。用户报告的 iPhone「能点但选中状态不变」尚未在物理设备复现，不能宣称已确定唯一根因。
- 换鸭局部更新，保留按钮节点与滚动位置；明确显示使用中、未解锁、保存失败。首页、局内、分享卡共用有效皮肤判断。
- 白／金鸭使用相同位图采样路径和缓存图集，标准合成保留透明度，不依赖 Canvas filter。旧档及异常未解锁金鸭回退白鸭。
- `npm run verify` 通过；新增星级边界单测和 Chromium 桌面／触控、WebKit 触控回归。验证真实像素差异、透明度、缓存、刷新保存和写入失败反馈。完整日志：`docs/skin-stars-verify.log`；截图及报告：`tools/e2e/shots/skin-stars/`。
- WebKit 是本机自动化，不能代替用户 iPhone。物理设备复核仍待用户使用候选确认。
- 只在 `codex/trusted-experience` 本地实现，没有推送或公开部署。
