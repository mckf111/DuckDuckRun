# 换鸭、星级与图鉴照片优化

## 星级与换鸭（2026-09-07）

- 通关总星数来自各关历史最高成绩，跨难度不重复累加；金鸭仍为 15 星解锁，不扣星数。选关页将最高星级和累计印章分开，鸭铺解释星级与本地存档，结算页区分本次、最佳与总数。
- 原桌面基线能切换存档，但整页重绘把焦点移到标题。原 iPhone「能点但选中状态不变」未在修复前的物理设备上重现，不能宣称已确定唯一根因；修复后的实际设备反馈见下方。
- 换鸭局部更新，保留按钮节点与滚动位置；明确显示使用中、未解锁、保存失败。首页、局内、分享卡共用有效皮肤判断。
- 白／金鸭使用相同位图采样路径和缓存图集，标准合成保留透明度，不依赖 Canvas filter。旧档及异常未解锁金鸭回退白鸭。
- `npm run verify` 通过；新增星级边界单测和 Chromium 桌面／触控、WebKit 触控回归。验证真实像素差异、透明度、缓存、刷新保存和写入失败反馈。完整日志：`docs/skin-stars-verify.log`；截图及报告：`tools/e2e/shots/skin-stars/`。
- WebKit 是本机自动化，不能代替用户 iPhone。2026-09-07，用户通过本轮同一 Wi-Fi 手机入口复核，在询问选中状态及首页／局内颜色的反馈中回复「切换正常」。这是本轮原始换鸭问题的实机确认；未提供机型、系统或浏览器版本，不补填。
- 只在 `codex/trusted-experience` 本地实现，没有推送或公开部署。

## 全图鉴照片与声明（2026-09-07）

- 全部 40 件均完成中文与别名检索、候选取舍；采用 30 张：23 准确实拍、3 现场纪实、1 复原展示、3 相关示意。10 件因准确对象、摄影来源或使用条件未核实而暂缺，具体原因及出处见 [ALBUM-REVIEW.md](ALBUM-REVIEW.md)。
- 旧梅花糕图片与南京梅花糕对象不符而停用；雨花茶不再用茶园冒充；日本金泽金箔示意停用。雨花石陈列、旺鸡蛋摊位、复原宝船、萤科形态示意等均明确分类，不计入准确实拍。
- 当前图片最长边不超过 1200px，保持原画面比例、未裁剪改色，移除 EXIF/XMP/IPTC 等非必要嵌入元数据。原始输入、API 及替换历史在本地 `docs/album-review`；可入库台账为 `assets/album/REVIEW.json`。
- 详情页同源按需加载、放大及关闭、失败重试、旧请求隔离已完成；隐藏条目未获得前不请求照片。资料页与图鉴共享照片记录，署名文件、README 统计和逐项报告可通过生成器检查一致性。
- 制品排除 18 张停用图鉴照片及候选原始记录，保留仍有代码引用的背景后备。台账共 88 项，其中 70 项随运行制品提供；公开发布审核仍未解除。

## 最终候选与验证

- 内部版本：`d03eeeaef6eb`，目录 `dist/releases/d03eeeaef6eb/`。本机 `http://127.0.0.1:8010/`，同一 Wi-Fi 手机 `http://192.168.0.102:8010/`；入口已回读确认。服务只提供 `dist`，未修改防火墙。
- 最终 `npm run verify` 完整通过；日志 `docs/album-review/final-verify.log`。包含逻辑、存档、全旅程模拟、源码与制品两套照片回归、确定性重建及资源预算。
- 照片回归覆盖桌面、390px 竖屏、360px 小屏、844px 横屏及 WebKit，共 40 条目；校验放大关闭、真实网络失败与重试、延迟请求、无第三方请求、署名链接与无横向溢出。
- 换鸭还验证首页、局内角色与分享卡真实像素变化、透明度保持、图集缓存和保存失败提示。一次完整回归曾出现滚动断言偏移；补充定位日志、正常及 50/150/300/650ms 字体延迟场景后均通过，严格滚动断言未放宽。该偶发自动化偏移未稳定复现，不宣称已确定其唯一原因。
- 本地无压缩请求：首屏 857 KiB，首关累计 1276 KiB，完整制品 11.77 MiB，分别低于 1.5/3/20 MiB 预算；这不代表大陆网络测速或长期实机性能结果。
- 画面与报告：`tools/e2e/shots/skin-stars/`、`tools/e2e/shots/album-photos/source/`、`tools/e2e/shots/album-photos/dist/`。用户实机反馈只对应换鸭问题，没有扩写为全部手机性能或图片验收。
